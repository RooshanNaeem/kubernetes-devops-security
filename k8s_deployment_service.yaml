apiVersion: apps/v1
kind: Deployment
metadata:
  name: devsecops-demo
  labels:
    app: devsecops-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: devsecops-demo
  strategy: {}
  template:
    metadata:
      labels:
        app: devsecops-demo
    spec:
      volumes:
      - name: vol
        emptyDir: {}  
      serviceAccountName: default
      containers:
      - name: devsecops-container 
        image: replace     
        volumeMounts:
        - mountPath: /tmp
          name: vol
        securityContext:
          runAsNonRoot: true
          runAsUser: 100
          readOnlyRootFilesystem: true
---       
apiVersion: v1
kind: Service
metadata:
  name: devsecops-svc
  labels:
    app: devsecops-demo
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: devsecops-demo
  type: NodePort